<UserControl x:Class="LibraryWpf.UserControls.IssueControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Выберите книгу:"/>
        <ComboBox x:Name="CbBooks" Grid.Row="1" Height="30" Margin="5"/>

        <TextBlock Grid.Row="2" Text="Выберите читателя:"/>
        <ComboBox x:Name="CbReaders" Grid.Row="3" Height="30" Margin="5" VerticalAlignment="Top"/>

        <Button Grid.Row="3" Content="Выдать" Height="35"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Left"
                Margin="5"
                Click="BtnIssue_Click"/>
    </Grid>
</UserControl>

