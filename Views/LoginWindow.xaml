<Window x:Class="LibraryWpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход" Height="230" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логин:"/>
        <TextBox x:Name="TbLogin" Grid.Row="1"/>

        <TextBlock Grid.Row="2" Text="Пароль:"/>
        <PasswordBox x:Name="TbPassword" Grid.Row="3" Height="30" VerticalAlignment="Top"/>

        <Button Grid.Row="3" Content="Войти" Height="35"
                VerticalAlignment="Bottom"
                Click="BtnLogin_Click"/>
    </Grid>
</Window>

